{
  "setupTasks": [
    "pip install -r requirements.txt"
  ],
  "tasks": {
    "install-dependencies": {
      "name": "Installing Screener Dependencies",
      "command": "pip install -r requirements.txt",
      "restartOn": {
        "files": ["requirements.txt"]
      }
    }
  }
}
